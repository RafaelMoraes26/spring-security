server:
  port: 8080

logging:
  level:
    org:
      springframework:
        security: DEBUG

spring:
  application:
    name: spring-security-auth
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
    platform: h2
    initialize: true
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: "Ov23liUt2wSh2vkeQMA9"
            client-secret: "52eb91e14d312345890e85598edab97deb21374c"
            scope: read:user, user:email
            redirect-uri: "{baseUrl}/login/oauth2/code/github"
            client-name: GitHub
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
            user-name-attribute: login

security:
  jwt:
    secret-key: 2d6a7c5ed3389c7fc80a27fe469c252ccbcf2b3f52847f2bb2fcc9a991b200c7
    expiration-time: 3600000
